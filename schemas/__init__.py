# schemas/ — NIKKO Agent Communication Protocol schema package.
# Canonical data contracts for all inter-agent payloads (ACP), retrieval
# adapters, and validation utilities. Imported by agents/, backend/,
# orchestration/, and retrieval/ packages.
#
# Moved from schemas/ during Phase 6 housekeeping — Python source
# belongs in an importable package, not a documentation directory.
