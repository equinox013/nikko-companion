# Nikko backend -- Python dependencies.
# Pin minor versions; patch versions float for security fixes.
# Sync with environment.yml if adding any package that overlaps with training deps.
#
# Phase 7 MVP runtime stack (all active):
#   - fastapi + uvicorn: ASGI server
#   - pydantic: ACP schema validation (docs/schemas/acp_schemas.py)
#   - httpx: HFSpaceFullGenerator blocking POST to HF Space /pipeline
#   - requests: PubMedAdapter (E-utilities) + WebSearchAdapter (page scraping)
#   - ddgs: DuckDuckGo search backend for WebSearchAdapter (G-RETRIEVAL-01)
#   - beautifulsoup4 + lxml: HTML parsing for WebSearchAdapter sanctioned-domain scrape
#
# NER-based PII redaction (SPEC-800 REQ-800-008 / REQ-800-011)
# en_core_web_sm must also be downloaded at deploy time — add to Render build command:
#   pip install -r backend/requirements.txt && python -m spacy download en_core_web_sm
spacy>=3.7,<4.0

# NOT needed in this container -- inference runs on HF Spaces ZeroGPU, not here.
# transformers, peft, torch, bitsandbytes are all omitted intentionally.

# API layer
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*

# HF Space bridge
httpx==0.27.*

# RAG / retrieval (NikkoPipeline steps 4-7)
requests==2.32.*
ddgs>=6.0
beautifulsoup4>=4.12
lxml>=5.0

# Semantic safety pre-filter (§8g Improvement 3, retrieval/semantic_safety_filter.py)
# faiss-cpu: FAISS flat index for cosine similarity lookup against crisis phrase DB.
# fastembed: ONNX-based embeddings (no PyTorch) — BGE-small-en-v1.5, ~67MB.
#   Replaces sentence-transformers which pulled PyTorch (~300MB) and OOMed
#   Render free tier (512MB). fastembed uses onnxruntime-cpu (~50MB) instead.
faiss-cpu>=1.8.0
fastembed>=0.3.0
