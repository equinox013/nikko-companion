# HF Space inference dependencies.
# Pinned to match the training stack (environment.yml / CLAUDE.md §8).
transformers==4.46.3
peft==0.13.2
accelerate==1.1.0
bitsandbytes==0.45.0
torch==2.5.1
fastapi==0.115.*
uvicorn==0.32.*
gradio==4.44.0
spaces
huggingface_hub>=0.26.0
